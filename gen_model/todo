upscale model from HPO to actual models (same best hyperparameters from LoRA HPO)

take models from upscaled and run autoregressive rollout of trajectories

switch visualization to py3dmol

visualization utils should pass forward seqres so that py3dmol can properly show the molecules

why are we training before HPO?

shouldn't tensorboard code to visualize be run before long training code cell starts?

make sure all necessary pip installs are defined for colab (like optuna)